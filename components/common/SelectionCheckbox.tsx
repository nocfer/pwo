/**
 * SelectionCheckbox — moved into the component kit at components/ui/Checkbox.
 * Re-exported here so existing `@/components/common` imports keep working.
 * @deprecated import { Checkbox } from '@/components/ui' instead.
 */

export { default } from '@/components/ui/Checkbox'

/**
 * SegmentedControl — moved into the component kit at components/ui/Segmented.
 * Re-exported here so existing `@/components/common` imports keep working.
 * @deprecated import { Segmented } from '@/components/ui' instead.
 */

export { default } from '@/components/ui/Segmented'

/**
 * UndoToast — moved into the component kit at components/ui/UndoToast.
 * Re-exported here so existing `@/components/common` imports keep working.
 * @deprecated import { UndoToast } from '@/components/ui' instead.
 */

export { default as UndoToast, default } from '@/components/ui/UndoToast'

/**
 * Button — moved into the component kit at components/ui/Button.
 * Re-exported here so existing `@/components/common` imports keep working.
 * @deprecated import from '@/components/ui' instead.
 */

export { default } from '@/components/ui/Button'
